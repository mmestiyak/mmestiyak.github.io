+++
title = "Projects"
description = "Production systems I've built and shipped"
# Rendered inside /work/, this section page itself never builds,
# so the /projects/ URL is free for the redirect to /work/.
[build]
render = "never"
+++
